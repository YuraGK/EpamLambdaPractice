## Pass back the result from DynamoDB. **
                #set($id = $ctx.result.id)
                #set($date = $ctx.result.createdAt)

                $util.toJson({
                    "data": {
                        "createEvent": {
                	        "id": $id,
                	        "createdAt": $date
                	    }
                	}
                })
                